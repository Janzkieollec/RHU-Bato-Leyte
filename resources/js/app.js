import Echo from 'laravel-echo';
window.Pusher = require('pusher-js');

window.Echo = new Echo({
    broadcaster: 'pusher',
    key: process.env.MIX_PUSHER_APP_KEY,
    cluster: process.env.MIX_PUSHER_APP_CLUSTER,
    forceTLS: true,  // Ensure TLS is enabled for secure WebSocket connection
    encrypted: true  // This should match your `useTLS` setting in broadcasting.php
});
